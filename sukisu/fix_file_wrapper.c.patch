commit 40631ecf47aaba5b69c5eccf52c97dc2419c2a54
Author: debiansid <debiansid@users.noreply.github.com>
Date:   Tue Nov 18 10:43:05 2025 +0800

    fix

diff --git a/kernel/file_wrapper.c b/kernel/file_wrapper.c
index 1ed0b40f..8d88e2e9 100644
--- a/kernel/file_wrapper.c
+++ b/kernel/file_wrapper.c
@@ -286,7 +286,7 @@ static int ksu_wrapper_clone_file_range(struct file *file_in, loff_t pos_in,
 	}
 	return -EINVAL;
 }
-#if LINUX_VERSION_CODE >= KERNEL_VERSION(4, 19, 0)
+#if LINUX_VERSION_CODE >= KERNEL_VERSION(4, 19, 0) && LINUX_VERSION_CODE < KERNEL_VERSION(6, 6, 0)
 static int ksu_wrapper_dedupe_file_range(struct file *src_file, loff_t loff,
 				struct file *dst_file, loff_t dst_loff, u64 len) {
 	// TODO: determine which file to use
@@ -297,7 +297,7 @@ static int ksu_wrapper_dedupe_file_range(struct file *src_file, loff_t loff,
 	}
 	return -EINVAL;
 }
-#else
+#elif LINUX_VERSION_CODE < KERNEL_VERSION(4, 19, 0)
 static ssize_t ksu_wrapper_dedupe_file_range(struct file *src_file, u64 loff,
 				u64 len, struct file *dst_file, u64 dst_loff) {
 	// TODO: determine which file to use
@@ -309,7 +309,7 @@ static ssize_t ksu_wrapper_dedupe_file_range(struct file *src_file, u64 loff,
 	return -EINVAL;
 }
 #endif
-
+#endif
 static int ksu_wrapper_release(struct inode *inode, struct file *filp) {
 	ksu_delete_file_wrapper(filp->private_data);
     return 0;
