diff --git a/kernel/sys.c b/kernel/sys.c
index 7595dc25d..4d548c77f 100644
--- a/kernel/sys.c
+++ b/kernel/sys.c
@@ -1297,7 +1297,9 @@ static int override_release(char __user *release, size_t len)
 	}
 	return ret;
 }
-
+#ifdef CONFIG_KSU_SUSFS_SPOOF_UNAME
+	extern void susfs_spoof_uname(struct new_utsname *tmp);
+#endif
 SYSCALL_DEFINE1(newuname, struct new_utsname __user *, name)
 {
 	struct new_utsname tmp;
@@ -1306,6 +1308,9 @@ SYSCALL_DEFINE1(newuname, struct new_utsname __user *, name)
 
 	down_read(&uts_sem);
 	memcpy(&tmp, utsname(), sizeof(tmp));
+#ifdef CONFIG_KSU_SUSFS_SPOOF_UNAME
+	susfs_spoof_uname(&tmp);
+#endif
 	up_read(&uts_sem);
 
 	rcu_read_lock();